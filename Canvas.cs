using System;
using SFML.Graphics;
using SFML.Audio;
using SFML.System;
using SFML.Window;
using System.Collections.Generic;

namespace ZelenskiyGame
{
    class Canvas:UI
    {
        //protected Label label;
        //public Canvas(RenderWindow window)
        //{
        //    label = new Label();
        //    label.text = new Text(label.clock.ElapsedTime.ToString(), label.font, 25);
        //    label.Text(label.text, new Vector2f(0, 0), window, Math.Round(label.clock.ElapsedTime.AsSeconds()).ToString() + " s");
        //}
    }
}
